apiVersion: apps/v1
kind: Deployment
metadata:
  name: jbosseap-app-migration-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: jbosseap-app-migration-operator
  template:
    metadata:
      labels:
        name: jbosseap-app-migration-operator
    spec:
      serviceAccountName: jbosseap-app-migration-operator
      containers:
        - name: ansible
          command:
          - /usr/local/bin/ao-logs
          - /tmp/ansible-operator/runner
          - stdout
          # Replace this with the built image name
          image: "quay.io/cadjai/jbosseapappmigration-operator:v0.0.2"
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
            readOnly: true
        - name: operator
          # Replace this with the built image name
          image: "quay.io/cadjai/jbosseapappmigration-operator:v0.0.2"
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "jbosseap-app-migration-operator"
      volumes:
        - name: runner
          emptyDir: {}
