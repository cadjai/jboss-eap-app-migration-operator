apiVersion: apps/v1
kind: Deployment
metadata:
  name: JBossEAP-App-Migration-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: JBossEAP-App-Migration-operator
  template:
    metadata:
      labels:
        name: JBossEAP-App-Migration-operator
    spec:
      serviceAccountName: JBossEAP-App-Migration-operator
      containers:
        - name: ansible
          command:
          - /usr/local/bin/ao-logs
          - /tmp/ansible-operator/runner
          - stdout
          # Replace this with the built image name
          image: "quay.io/cadjai/jbosseapappmigration-operator:v0.0.1"
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
            readOnly: true
        - name: operator
          # Replace this with the built image name
          image: "quay.io/cadjai/jbosseapappmigration-operator:v0.0.1"
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "JBossEAP-App-Migration-operator"
      volumes:
        - name: runner
          emptyDir: {}
